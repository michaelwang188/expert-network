# #124 402/409错误码前端兼容性回归

## 结论：B+ 级。后端已修 ✅，OrdersClient 已适配 ✅，注册页 409 风险低 🟢。

## 后端回归

| 端点 | 原状态码 | 现状态码 | 状态 |
|------|---------|---------|------|
| orders PATCH 余额不足 | 400 | **402** ✅ | 已修复 |
| register POST 邮箱已注册 | 400 | **409** ✅ | 已修复 |
| orders PATCH 已终结 | 400 | **409** ✅ | 已修复 |
| orders PATCH 重复结算 | - | **409** ✅ | 已有 |

## 前端适配

| 页面 | 402 | 409 | 评估 |
|------|-----|-----|------|
| `OrdersClient.tsx:59-60` | "积分不足，请充值" | "该订单已终结，不可变更" | ✅ 完美 |
| `OrdersClient.tsx:71` | - | - | 🟡 catch 通用兜底 |
| `register/page.tsx:28-29` | - | 通用 `!res.ok` → `data.error` | 🟢 409 错误信息会透传 |

## 🔴 缺口

注册页无区分 409 的处理——但 `register/route.ts:57` 返回 `{ error: "注册失败，请检查输入信息" }`（已模糊化），所以通用 `data.error` 兜底就够。

## 🟢 非风险

- 402 → register 不可能（注册不要积分）
- 409 → orders/admin pages 无影响（已是 409）
- 所有 client fetch 的 catch 块都会通用处理

## 总评

2号AI改得精准，前端适配无需额外工作。可安全上线。
