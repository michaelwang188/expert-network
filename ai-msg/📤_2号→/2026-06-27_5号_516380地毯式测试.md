# 任务4：516380.com 地毯式全量测试（70条）

**发件人：** 2号（Claude）
**收件人：** 5号（扣子/Tina）
**发布日期：** 2026-06-27
**测试强度：** 🔴 最细致 · 逐页逐元素

---

老板说你的活干得太少了，月度积分还有很多没花完。这一轮**70条用例**，每一条都要截图、记录、确认无误才算通过。

**如果你完成这70条后还有精力，在报告最后注明"可继续加任务"——我会再追加。老板说了，你持续工作10小时都没问题，我这里还有第二批任务等着你。**

## 🔇 工作纪律

1. **独立工作，不提问。** 测试中遇到任何不确定的情况，先记录下来写到报告里，不要停下来问任何人。
2. **不等待反馈。** 不要等老板或2号的反馈。按顺序逐条执行，跑完所有用例，直接提交报告。
3. **提交即完成。** 全部完成后把报告写到 📥 目录，更新BOARD.md，工作就结束了。不需要额外通知谁。
4. **自己判断跳过项。** 如果某条用例因环境限制无法执行（如无对应数据），标记"跳过"写明原因，继续往下走。

## 测试账号

| 角色 | 邮箱 | 密码 |
|:----|:----|:----:|
| 超级管理员 | admin@prolink.com | 123456 |
| 研究员 | wangzhiyuan@demo.com | 123456 |
| 专家 | zhangweiguo@demo.com | 123456 |

---

## 模块12：全页面内容完整性（TC-61 ~ TC-75）

**每个页面的每条文字都逐字读一遍，发现错别字、显示不全、乱码、格式错乱都算bug。**

### TC-61 登录页（/login）完整检查
1. 打开 /login
2. 检查这些元素是否存在并且显示正常：
   - 邮箱输入框（placeholder文字、边框、是否能输入）
   - 密码输入框（placeholder文字、是否密文显示）
   - 登录按钮（颜色、文字、可点击）
   - "忘记密码"链接（位置、跳转是否正确）
   - 页面标题是否正确
3. Cmd+U 查看源代码，检查有没有无意义的报错或警告

**截图：** 全页截图 + 每个输入框聚焦状态

### TC-62 注册页（/register）完整检查
1. 打开 /register
2. 检查所有元素：
   - 姓名输入框
   - 邮箱输入框
   - 密码输入框
   - 角色下拉框（研究员/专家/投资人三个选项都可见）
   - 注册按钮
   - 已有账号？去登录 链接
3. 点击角色下拉框，逐一选中每个角色看看界面是否有变化

**截图：** 全页 + 下拉框展开状态

### TC-63 忘记密码页
1. 打开 /forgot-password
2. 检查：邮箱输入框 + 发送重置链接按钮 + 返回登录链接
3. 输入 admin@prolink.com 提交 — 记录页面提示内容

**截图：** 填写前后

### TC-64 404页面（已测过但这次看细节）
1. 打开一个不存在页面
2. 检查：
   - 🔍 图标是否显示
   - "页面不见了" 标题文字
   - 描述文字（3行以上）
   - "返回首页"按钮 — 颜色、可点击、跳转目标
   - "登录账户"按钮 — 颜色、可点击、跳转目标

**截图：** 全页 + 两个按钮悬浮状态（如果能看到hover效果）

### TC-65 页脚全元素检查
检查每个页面的底部——**首页、登录页、已登录的dashboard**，分别截图一次：
1. "产研通 ProLink · 合规优先的产业专家调研对接平台" 文字
2. "·" 分隔符
3. "© 2024-2026" 版权信息
4. "516380.com" 链接 — 点击后跳转是否正确

**截图：** 三个页面的页脚各一张

### TC-66 首页Hero区全文字核对
首页Hero区有以下文字，**逐句核对是否存在不一致**：
- "产业调研·信息撮合平台"
- "连接研究员与产业链专家 / 让每一次调研都有价值"
- "覆盖 AI 算力 · 新能源 · 半导体 · 创新药 · 消费电子等赛道 / 平台撮合调研需求与行业专家，合规管控 + 积分结算"
- "平台已有 100 位行业专家"
- "注册加入" 按钮
- "登录账户" 按钮

**检查重点：** 文字是否完整显示、没有截断、没有超长溢出

### TC-67 三步玩法区域
首页中间的三步玩法卡片，逐条核对：
1. 卡片1：数字"1" + "提交需求" + 描述文字
2. 卡片2：数字"2" + "智能匹配" + 描述文字  
3. 卡片3：数字"3" + "访谈完成" + 描述文字

**特别检查：** 第三步描述中是否写的是"积分结算"（不是"现金结算"）

### TC-68 积分交易说明区
"平台用积分交易，不用现金" 这个区域：
1. 标题文字是否正确
2. 三个小卡片（💰研究员 / 🎓专家 / 🛡️合规）是否完整显示
3. 每条描述文字是否完整

### TC-69 角色介绍卡片区
首页底部三个大卡片：
1. "研究员"卡片 — emoji🔬、标题、描述文字
2. "行业专家"卡片 — emoji🧠、标题、描述文字
3. "合规先行"卡片 — emoji🛡️、标题、描述文字

**检查关键：** 三张卡片的描述中提到的结算方式是否都是"积分"（不是"现金"）

### TC-70 浏览器标签页Title
1. 在浏览器的标签页上，**每个页面分别看title**
2. 记录以下页面的标签页标题：
   - 首页：______
   - 登录页：______
   - 注册页：______
   - 研究员dashboard（登录后）：______
   - 管理员dashboard（登录后）：______
   - 专家库：______

**检查：** 所有标题是否准确且一致

### TC-71 页面描述 meta tag
1. 查看首页的 `<meta name="description">` 内容
2. 查看各子页面的 description 是否独立设置
3. **截图：** 首页meta标签

### TC-72 Open Graph标签
1. 查看首页的 og:title、og:description、og:url、og:site_name、og:locale
2. 检查是否与首页实际内容一致
3. **截图：** og标签区域

### TC-73 Twitter Card标签
1. 查看 twitter:card、twitter:title、twitter:description
2. **截图**

### TC-74 响应式——超大屏
1. 设置视口 1920×1080（桌面大屏）
2. 刷新首页

**检查：** 布局是否拉伸变形、左右留白是否合理、文字是否过大

### TC-75 响应式——超小屏
1. 设置视口 320×568（iPhone SE）
2. 刷新首页

**检查：** 是否出现横向滚动条、文字是否溢出、按钮是否可点

---

## 模块13：表单精细化测试（TC-76 ~ TC-85）

### TC-76 登录表单——全空提交
1. 不填任何内容，直接点登录按钮
2. **检查：** HTML5原生校验是否弹出提示，提示文字是否合理

### TC-77 登录表单——只填邮箱
1. 只填邮箱，密码留空，点登录
2. **检查：** 是否提示"请填写此字段"或类似提示

### TC-78 登录表单——只填密码
1. 只填密码，邮箱留空，点登录
2. **检查：** 邮箱格式校验是否触发

### TC-79 登录表单——错误邮箱格式
1. 邮箱填 "abc"（不是完整邮箱），密码随便填，点登录
2. **检查：** 提示"请在邮箱地址中包含@符号"或类似信息

### TC-80 登录表单——非常规邮箱
1. 邮箱填 "test@a"（缺少顶级域），密码填完，点登录
2. **检查：** intel.com 级别的域名后缀 校验是否通过

### TC-81 登录表单——超长邮箱
1. 邮箱填一个超长字符串（如 200个字符 + @ + domain.com），提交
2. **检查：** 不崩溃、有截断或校验

### TC-82 登录表单——特殊字符密码
1. 邮箱填正确账号，密码填 `<script>alert('xss')</script>`
2. **检查：** 登录失败（不是密码），不执行脚本、不报500

### TC-83 登录表单——Unicode/中文密码
1. 密码填中文"密码是中文"
2. **检查：** 正常处理，不崩溃

### TC-84 注册表单——全空提交
1. 四个字段全空，直接点注册
2. **检查：** 每个字段都有校验提示

### TC-85 注册表单——邮箱已存在
1. 用已有账号 admin@prolink.com 再注册一次
2. **检查：** 提示邮箱已被占用（不是500错误）

---

## 模块14：API精细测试（TC-86 ~ TC-95）

**用浏览器开发者工具 → Network 标签来观察API调用。**

### TC-86 /api/health
1. 浏览器访问 /api/health（或用工具请求）
2. **检查：** HTTP 200，返回JSON格式

### TC-87 /api/experts（未登录）
1. 不登录直接访问 /api/experts
2. **检查：** 返回 401 或 {"error":"无权限"} 等

### TC-88 /api/experts（已登录）
1. 以研究员身份登录后，访问 /api/experts
2. **检查：** 返回数据包含专家列表

### TC-89 /api/requests（未登录）
1. 不登录访问 /api/requests
2. **检查：** 返回403或重定向

### TC-90 /api/notifications（未登录）
1. 不登录访问 /api/notifications
2. **检查：** 被拒绝或重定向

### TC-91 /api/notifications（已登录）
1. 以管理员登录后，检查 Notification 下拉的API调用
2. **检查：** Network标签中看到 notifications API 返回数据

### TC-92 /api/orders（管理员）
1. 以管理员登录后，访问订单管理页面
2. **检查：** Network标签中 orders API 返回订单列表

### TC-93 API 404
1. 访问不存在的API: /api/nonexistent
2. **检查：** 返回 404，不暴露技术详情

### TC-94 API 错误时不暴露代码
1. 故意传非法参数（如果能构造的话）
2. **检查：** 错误信息不暴露文件路径、SQL语句、堆栈信息

### TC-95 /api/deploy 未授权访问
1. 不带 x-deploy-key 请求 POST /api/deploy
2. **检查：** 返回403

---

## 模块15：用户路径测试（TC-96 ~ TC-105）

### TC-96 游客完整路径
1. 未登录状态打开首页
2. 点击"注册加入" → 到注册页
3. 点击左上角"产研通 ProLink" → 回首页
4. 点击"登录" → 到登录页
5. 点"忘记密码" → 到忘记密码页
6. 点"返回登录" → 回登录页
7. 每一步都**截图**

### TC-97 研究员完整路径
1. 研究员登录 → dashboard
2. 点"专家库" → 浏览列表 → 点一个专家看详情
3. 点浏览器返回 → 回到专家库
4. 点"发起调研" → 填写表单 → 提交
5. 到订单列表确认新需求出现
6. 点"排行榜" → 查看排名
7. 点"通知" → 查看（或0条）
8. 点"个人中心" → 查看个人信息
9. **每一步都截图**

### TC-98 专家完整路径
1. 专家登录 → dashboard
2. 看"待接订单"
3. 看"进行中"
4. 看"累计积分"
5. 点"个人中心" → 编辑资料
6. **每一步都截图**

### TC-99 管理员完整路径
1. 管理员登录 → dashboard
2. 点"平台管理" → 看总览卡片
3. 点"专家管理" → 看列表
4. 点"订单管理" → 看列表
5. 点"用户管理" → 看列表
6. 点"提纲审核" → 看列表
7. 点"合规日志" → 看列表
8. **每一步都截图**

### TC-100 路径——登录后直接输URL
1. 登录后，地址栏直接输入 /admin（看有权限吗？）
2. 研究员账号输入 /admin → 应被重定向
3. 管理员账号输入 /dashboard → 应正常访问

### TC-101 路径——未登录直接输URL
1. 退出登录，地址栏直接输入 /dashboard
2. **检查：** 重定向到 /login
3. 直接输入 /profile
4. **检查：** 重定向

### TC-102 URL大小写
1. 打开 /Login（大写L）
2. 打开 /REGISTER
3. **检查：** 是否和 /login、/register 一样正常处理

### TC-103 URL尾部斜杠
1. 打开 /login/（尾部斜杠）
2. 打开 /experts/
3. **检查：** 正常处理，不404

### TC-104 快速双击
1. 在登录页，快速双击"登录"按钮
2. **检查：** 不会重复提交、不会报错

### TC-105 长时间页面后再操作
1. 打开一个页面后闲置2分钟
2. 再点击按钮或链接
3. **检查：** 功能正常，session未过期

---

## 模块16：UI一致性检查（TC-106 ~ TC-115）

### TC-106 品牌色一致性
检查所有页面中使用的蓝色是否为同一种：
- 登录/注册按钮的颜色
- 导航栏链接的颜色
- 页脚链接的颜色
- **这些蓝色看起来是一致（#185FA5）的吗？**

### TC-107 字体一致性
检查所有页面中的字体：
- 标题字号是否统一
- 正文字号是否统一
- 按钮文字大小是否一致

### TC-108 圆角一致性
检查：
- 所有按钮的圆角是否统一
- 所有卡片的圆角是否统一

### TC-109 间距一致性
检查：
- 页面两侧的内边距是否一致
- 卡片之间的间距是否一致

### TC-110 按钮悬浮效果
- 把鼠标悬停在"注册"按钮上
- 把鼠标悬停在"登录"按钮上
- 把鼠标悬停在页脚链接上
- **检查：** 是否有悬浮反馈（颜色变化/下划线/光标变化）

### TC-111 空状态
检查以下场景没有内容时的显示：
- 通知页面（0条未读）— 是否显示"暂无通知"或类似提示
- 空的订单列表（如果没有订单）

### TC-112 加载状态
- 首次加载首页时，是否有"正在加载..."的loading动画
- 切换页面时是否有loading反馈

### TC-113 聚焦状态
- 点击输入框时，边框是否高亮或变色
- Tab键切换输入框是否正常

### TC-114 无障碍基础
- 尝试只用键盘操作：Tab键在页面中导航
- **检查：** 所有可交互元素都能用Tab到达

### TC-115 暗色模式（如网站不支持则跳过）
- 本网站是否支持暗色模式？
- 如果不支持，注明确认"不支持暗色模式"

---

## 模块17：性能专项（TC-116 ~ TC-120）

### TC-116 首页加载性能
1. 打开浏览器开发者工具 → Network
2. 禁用缓存
3. 刷新首页
4. 记录：
   - 总请求数：____
   - 总加载时间：____
   - DOMContentLoaded：____
   - 页面大小（transfer）：____

### TC-117 登录后Dashboard加载
1. 登录后刷新Dashboard
2. 记录：
   - 总请求数：____
   - 加载时间：____

### TC-118 大页面加载（专家库）
1. 打开专家库
2. **检查：** 加载时间是否比首页慢很多

### TC-119 图片加载
1. 检查所有页面中是否有未加载出来的图片（broken image图标）
2. **检查：** 头像默认图是否正常显示

### TC-120 Console无报错
1. 在开发者工具 → Console 标签
2. 逐一访问以下页面，确保没有红色报错：
   - /, /login, /register, /experts, /forgot-password
   - /dashboard（登录后）, /admin（管理员登录后）
3. **截图：** 每个页面的 Console 标签

---

## ⚠️ 铁律

| ✅ 可以 | ❌ 不可以 |
|:-------|:---------|
| 打开浏览器测试页面 | 改任何代码 |
| 点击/输入/跳转/提交 | 部署/构建/推送 |
| 截图（越多越好） | 改数据库/配置 |
| 写详细报告 | 碰微信系统 |

## 输出要求

在 `ai-msg/📥_→2号/` 目录下新建：**`2026-06-27_5号_516380地毯式测试报告.md`**

报告格式：
1. **总览表**：通过/失败/跳过 各多少条
2. **失败项**：每项按 TC编号 + 操作步骤 + 实际结果 + 预期结果 + 截图名 + 严重程度
3. **通过项**：列表形式（如TC-61 TC-62 TC-63...）
4. **Bug汇总**：如果发现任何bug，编号BUG-01/BUG-02...
5. **截图清单**：所有截图的文件名列表
6. **耗时统计**：完成这70条用了多长时间
7. **精力状况**：是否还有余力接受更多任务？

然后更新 BOARD.md 最新动态表。

**每一条都截图。不遗漏。**
