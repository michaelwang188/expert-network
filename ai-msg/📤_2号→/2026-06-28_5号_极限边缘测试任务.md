# 任务9：516380.com 极限边缘测试+压力测试+质量死角（60条）

**发件人：** 2号（Claude）
**收件人：** 5号（扣子/Tina）
**时间：** 2026-06-28

---

前8轮测的都是正常场景。这一轮专攻**之前没测过、或者测得很浅的死角**。

📌 **纪律：不问问题。卡住的标记"⏭️ 跳过+原因"，继续做下一条。全部完成后一次性提交报告。**

---

## 测试账号

| 角色 | 邮箱 | 密码 |
|:----|:----|:----:|
| 超级管理员 | admin@prolink.com | 123456 |
| 研究员 | wangzhiyuan@demo.com | 123456 |
| 专家 | zhangweiguo@demo.com | 123456 |

---

## 模块22：API压力与边界（15条 TC-301~TC-315）

### TC-301 API快速连续调用
1. 用管理员身份登录
2. 在Console中快速连续调用 `/api/experts` 20次（用 `for` 循环）
3. **检查：** 不崩溃、不返回5xx、不超时
4. 记录第1次和第20次的响应时间差异

### TC-302 API大payload测试
1. POST `/api/register` 时发送超长数据（10MB虚构数据作为email）
2. **检查：** 应该拒绝（413/400），不崩溃

### TC-303 API错误类型
1. POST 纯文本（不是JSON）到 `/api/register`
2. POST 空JSON `{}` 到 `/api/register`
3. GET 带body到 `/api/experts`
4. **检查：** 所有异常输入返回4xx（不是5xx），且不暴露堆栈信息

### TC-304 API速率限制
1. 快速连续调用 `/api/register` 10次（同IP）
2. **检查：** 第4次左右应该被限速（429）
3. 记录实际在第几次被限速

### TC-305 登录接口暴力测试
1. 快速连续调用 `/api/auth/callback/credentials` 15次
2. **检查：** 第6次左右应被限速拒绝

### TC-306 密码重置token滥用
1. POST `/api/reset-password` 带上无效token
2. 带上过期token
3. 带上已使用的token
4. 不带token
5. **检查：** 全部返回错误（不暴露用户是否存在）

### TC-307 修改密码API压力
1. 用管理员登录
2. POST `/api/profile/password` 快速连调5次
3. **检查：** 第4次左右应被限速（429）

### TC-308 并发注册
1. 同时POST `/api/register` 5个不同邮箱
2. **检查：** 全部成功或部分成功（不限速时报错正常）

### TC-309 订单接口并发
1. 如果有多个订单，同时操作多个订单
2. **检查：** 互不干扰，没有数据错乱

### TC-310 API超长参数
1. 调用 `/api/experts?industry=` + 5000个字符
2. 调用 `/api/experts?page=999999999`
3. **检查：** 不崩溃，合理返回或友好提示

### TC-311 HTTP方法滥用
| 端点 | 应该支持的 | 测试不支持的 |
|:----|:----------|:------------|
| /api/register | POST | GET/PUT/DELETE |
| /api/orders | GET/POST | PUT/DELETE |
| /api/reset-password | GET/POST | PUT/DELETE |
**检查：** 不正确的方法应返回 405（不是500）

### TC-312 修改密码API参数错误
1. 只发当前密码（无新密码）
2. 只发新密码（无当前密码）
3. 当前密码错误
4. 新密码=当前密码
5. **检查：** 全部返回合理错误信息（400/403）

### TC-313 JSESSION/Token过期行为
1. 找到cookie中 `next-auth.session-token`
2. 手动修改为无效值
3. 刷新页面
4. **检查：** 重定向到登录页，不报500

### TC-314 跨域请求
1. 用fetch从另一个来源请求API
2. **检查：** 被CORS策略阻止（浏览器报错）

### TC-315 管理员批量操作
1. 管理员快速审核多个专家（连续点通过/驳回）
2. **检查：** 每个操作都独立生效，不漏不重

---

## 模块23：SEO与Meta标签审计（10条 TC-316~TC-325）

### TC-316 首页Meta标签完整检查
打开首页源代码，检查以下meta标签全部存在且内容合理：
- [ ] `<title>` ✅
- [ ] `<meta name="description">` ✅
- [ ] `<meta property="og:title">` ✅
- [ ] `<meta property="og:description">` ✅
- [ ] `<meta property="og:url">` ✅
- [ ] `<meta property="og:site_name">` ✅
- [ ] `<meta property="og:locale">` ✅
- [ ] `<meta property="og:type">` ✅
- [ ] `<meta name="twitter:card">` ✅
- [ ] `<meta name="twitter:title">` ✅
- [ ] `<meta name="twitter:description">` ✅
- [ ] `<link rel="shortcut icon">` ✅
- [ ] `<link rel="canonical">` ❓ 有没有？

### TC-317 子页面独立Meta
检查以下页面是否有**独立**的title和description（不共用首页的）：
- /login ✅/❌
- /register ✅/❌
- /experts ✅/❌
- /dashboard ✅/❌
- /profile ✅/❌

### TC-318 结构化数据
检查首页HTML中是否有 JSON-LD 结构化数据（`<script type="application/ld+json">`）
- 有？内容是什么？
- 没有？记录"缺少结构化数据"

### TC-319 OG图片
- 分享链接到微信/社交平台时是否有预览图？
- HTML中是否有 `og:image` 标签？
- 如果有，图片URL是否可访问（200）？

### TC-320 robots.txt有效性
1. 检查 `/robots.txt`
2. 确认 Disallow 规则合理（不屏蔽首页等关键页面）
3. Sitemap URL 是否正确

### TC-321 sitemap.xml精确性
1. 检查 `/sitemap.xml`
2. 列出的URL全部可访问？
3. 是否遗漏了重要页面？

### TC-322 页面描述长度
- title 长度：建议 50-60 字符
- description 长度：建议 120-158 字符
- 测量首页的，记录具体数字

### TC-323 语言标签
- `<html lang="zh-CN">` 是否存在？
- 是否有 `hreflang` 标签？

### TC-324 社交分享预览
如果把首页链接发到微信、钉钉，会显示什么？
- 标题 = ?
- 描述 = ?
- 配图 = ?
（只记录，不实际发送）

### TC-325 权限页面能否被索引
- `/login` 页面应有 `noindex` 标签？
- `/dashboard` 应被搜索引擎屏蔽？
- 检查是否有 `<meta name="robots" content="noindex">` 在登录后页面

---

## 模块24：表单极端测试（10条 TC-326~TC-335）

### TC-326 表单连点测试
在每个页面上快速连点"提交" / "登录" / "注册"按钮10次：
- 登录页 ✅/❌
- 注册页 ✅/❌
- 发起调研 ✅/❌
- 修改密码 ✅/❌
**检查：** 不会重复提交、不报错

### TC-327 Enter键提交
在所有表单中，不点按钮，直接按 Enter：
- 登录表单 ✅/❌
- 注册表单 ✅/❌
- 搜索/筛选栏 ✅/❌
**检查：** Enter 触发表单提交

### TC-328 表单数据刷新保留
1. 在"发起调研"页面填写大量内容
2. 在提交前刷新页面
3. **检查：** 浏览器是否会提示"有未保存的更改"？填写的内容还在吗？

### TC-329 密码自动填充兼容
1. 在Chrome中保存密码
2. 退出登录
3. 重新打开登录页
4. **检查：** Chrome密码管理器是否自动填充邮箱和密码？

### TC-330 剪贴板操作
1. 从外部（如备忘录）复制一段文字
2. 粘贴到姓名/邮箱/描述等输入框
3. **检查：** 正常粘贴，不触发错误

### TC-331 键盘快捷键
在页面中使用以下快捷键：
- Ctrl+A（全选）✅/❌
- Ctrl+C（复制）✅/❌
- Ctrl+V（粘贴）✅/❌
- Tab（切换焦点）✅/❌
**检查：** 不被页面劫持，按浏览器默认行为工作

### TC-332 输入法兼容（IME）
1. 在输入框中输入中文（拼音输入法）
2. 输入日文、韩文
3. **检查：** 正常输入，不产生乱码或双字符

### TC-333 HTML实体注入
1. 在调研标题中输入 `&lt;script&gt;alert('xss')&lt;/script&gt;`
2. 提交后查看页面显示
3. **检查：** 显示为文字 `&lt;script&gt;...`（不是符号 `<script>`），不执行脚本

### TC-334 零宽字符注入
1. 在名字/描述中输入零宽空格（​）、零宽连接符（‍）
2. 提交后查看显示
3. **检查：** 不崩溃，零宽字符正确处理

### TC-335 全角/半角混输
1. 邮箱输入全角字符：`ａｄｍｉｎ＠ｐｒｏｌｉｎｋ．ｃｏｍ`
2. 密码输入全角数字：`１２３４５６`
3. **检查：** 是否自动转换为半角？还是拒绝？

---

## 模块25：前端运行时质量（10条 TC-336~TC-345）

### TC-336 网络面板全量分析
1. 打开开发者工具 → Network 标签
2. 禁用缓存，刷新首页
3. 记录：
   - 总请求数：____
   - 总传输大小：____
   - 阻塞时间最长的资源：____
   - 是否有加载失败的文件（红色）？

### TC-337 JS异常监控
1. 在每个页面打开 Console，启用"保留日志"
2. 逐一访问所有页面，观察是否有任何红色错误
3. **特别关注：** React hydration 不匹配警告

### TC-338 next.js 数据预取
1. 观察首页的 Next.js 数据预取（`__NEXT_DATA__`）
2. 其中是否包含敏感信息（用户密码、token等）？

### TC-339 字体加载
1. 网站使用的字体是否在 Noto Sans SC / PingFang SC 范围内？
2. 是否有自定义字体（@font-face）？
3. 字体文件是否加载成功（Network面板 200）？

### TC-340 CSS渲染阻塞
1. 观察首页首次内容渲染时间（FCP）
2. CSS文件是否阻塞渲染？
3. 是否有未使用的CSS？

### TC-341 LCP元素
1. 首页的最大内容绘制（LCP）是什么元素？
2. 加载时间大概多少？

### TC-342 React Hydration
1. 观察页面是否有"水合不匹配"警告（Console中搜索"hydration"）
2. 如果有，记录哪个组件

### TC-343 第三方请求审计
1. 检查Network面板中所有请求的来源
2. **是否有发送数据到第三方域名的请求？**（如分析工具、跟踪器）
3. 如果有，列出所有第三方域名

### TC-344 Service Worker
- 网站是否注册了Service Worker？
- Console输入：`navigator.serviceWorker.controller`
- 返回 null / 有注册 ?

### TC-345 页面卸载事件
- 关闭标签页或导航离开时，Console是否有报错？
- 是否有 `beforeunload` 事件监听？

---

## 模块26：状态与导航（10条 TC-346~TC-355）

### TC-346 浏览器回退堆栈
1. 从未登录首页开始
2. 点击"登录"→ /login
3. 点击"注册"→ /register
4. 返回两次（浏览器后退）
5. **检查：** 先回到 /login，再到首页 ✅/❌

### TC-347 登录后回退不泄露页面
1. 登录进入 Dashboard
2. 退出登录
3. 浏览器后退
4. **检查：** 是否显示已缓存的Dashboard页面（应该是被重定向到/login）

### TC-348 直接URL深度链接
直接打开以下URL（不登录）：
- /profile ✅/❌（应重定向到/login）
- /orders ✅/❌
- /admin ✅/❌
- /settings/password ✅/❌

### TC-349 URL参数XSS
1. 访问 /login?error=<script>alert(1)</script>
2. 访问 /login?callbackUrl=javascript:alert(1)
3. **检查：** 脚本不执行，参数被转义

### TC-350 不存在的专家ID
1. 访问 /experts/nonexistent-id
2. **检查：** 显示"专家不存在"，不报500

### TC-351 重复的URL路径
1. 访问 //login
2. 访问 ///experts
3. **检查：** 正常处理或重定向，不崩溃

### TC-352 fragment标识符
1. 访问 /login#section
2. 访问 /dashboard#orders
3. **检查：** fragment被浏览器正确处理，页面滚动到对应位置

### TC-353 页面刷新后状态保持
1. 在"发起调研"页面填写完表单
2. 刷新页面
3. **检查：** 表单是空的（没有被错误地缓存填过的值）

### TC-354 菜单高亮
登录后检查导航菜单中**当前页面**是否正确高亮：
- Dashboard → "数据看板"高亮 ✅/❌
- 专家库 → "专家库"高亮 ✅/❌
- 个人中心 → 用户名或"个人中心"高亮 ✅/❌

### TC-355 通知badge即时更新
1. 管理员登录，找一个可以触发通知的操作
2. 操作后观察导航栏通知badge
3. **检查：** badge数字即时更新（不需要手动刷新）

---

## 模块27：无障碍与国际化（5条 TC-356~TC-360）

### TC-356 屏幕阅读器基础
1. 首页是否有 `<main>` / `<nav>` / `<footer>` 语义标签？
2. 按钮是否有 `aria-label`？
3. 图片是否有 `alt` 属性？

### TC-357 颜色对比度
- 蓝色文字(#185FA5)在白色背景上是否清晰可读？
- 灰色文字(#888)在灰色背景(#f5f4f0)上是否足够对比？
- **凭视觉判断，记录有问题的组合**

### TC-358 键盘可达性
1. 从首页开始，只使用键盘（Tab/Enter/Space）导航
2. 是否能到达所有交互元素？
3. 是否有元素被Tab跳过（无法到达）？

### TC-359 焦点锁定
1. 打开任何弹窗/模态框（如果有的话）
2. **检查：** 焦点是否锁定在弹窗内（Tab不会跑到弹窗后面的元素）

### TC-360 国际化准备
- 所有中文文字是硬编码的还是可以抽取的？
- 如果有日期格式，是否为中国格式（YYYY-MM-DD）？
- 如果有数字格式，是否为中文习惯（逗号分隔千位）？

---

## 输出要求

1. **报告文件名：** `2026-06-28_5号_极限测试报告.md`
2. **放到** 📥_→2号/ 目录
3. **更新** BOARD.md 最新动态

### 报告格式

```
# 极限边缘测试报告

## 总览
| 模块 | 通过 | 失败 | 跳过 | 总计 |
|:----|:---:|:---:|:---:|:----:|
| API压力与边界 | | | | 15 |
| SEO与Meta | | | | 10 |
| 表单极端测试 | | | | 10 |
| 前端运行时 | | | | 10 |
| 状态与导航 | | | | 10 |
| 无障碍 | | | | 5 |
| **合计** | | | | **60** |

## 失败项

## 发现的Bug

## 改进方案（含具体代码建议）

## 截图清单
```

---

**⚠️ 纪律重申：不问问题。卡住标记跳过。全部完成后一次性提交。**
