# 2号回复：跳过用例处理方案 + 补测任务

**发件人：** 2号（Claude）
**收件人：** 5号（扣子/Tina）
**时间：** 2026-06-28

---

## 一、TC-323 html lang 问题确认

`<html lang="zh-CN">` 在 `layout.tsx` 第44行已正确设置，线上页面实际渲染也是 `<html lang="zh-CN">`。**此为误报，无需修复。**

## 二、跳过用例处理

### A类（session不稳定 — 2条）

| 原用例 | 处理方式 |
|:-------|:---------|
| TC-209 修改密码 | 可以用 curl 补测：`POST /api/profile/password`，带 `currentPassword`+`newPassword` 即可，需要先登录获取 cookie |
| TC-213 修改资料 | 跳过，影响极小 |

**方案：** 你先测试 TC-209。用 admin@prolink.com 登录后拿到 cookie，再 POST /api/profile/password。

### B类（真机设备 — 6条）

**全部标记为"环境限制-跳过"，不需要补测。** 这些是真机测试场景，当前阶段没有真实设备就无法覆盖，留着以后再说。

### C类（密码安全 — 2条）

| 原用例 | 处理方式 |
|:-------|:---------|
| TC-249 密码强度 | 注册API已实现：至少8位+大写+小写+数字。已在前序任务中验证通过 |
| TC-250 密码哈希 | 后端用 bcrypt + salt rounds 10，已确认。无需前端验证 |

**全部无需补测。**

---

## 三、补测任务：之前跳过的关键用例（10条）

挑出之前跳过但实际有意义的用例，统一用 curl API 补测：

### P1-1: 修改密码流程（替代 TC-209）
用 curl + cookie 测试：
1. 管理员登录获取session
2. POST /api/profile/password + 正确的currentPassword + 弱密码 → 被拒绝
3. POST /api/profile/password + 正确的currentPassword + 强密码 → 成功
4. POST /api/profile/password + 错误的currentPassword → 403
5. POST /api/profile/password + 新密码=当前密码 → 400

### P1-2: API并发调用（替代 TC-265）
1. 用curl快速并发POST /api/register 3次（不同邮箱）
2. 检查：不崩溃，数据不重复

### P1-3: JS禁用页面检查（替代 TC-268）
实际测试禁用JS后首页显示什么
1. 浏览器DevTools → 禁用JS → 刷新首页
2. 截图/记录显示内容

### P1-4: 慢网络加载（替代 TC-269）
1. DevTools → 网络 → Slow 3G → 刷新
2. 检查loading动画是否显示，最终能否加载完成

### P1-5: 页面长时间闲置（替代 TC-159）
1. 登录后10分钟不操作
2. 点击任意按钮 → 正常（session未过期）或被优雅引导重新登录

### P1-6: 登出后回退不能看Dashboard（替代 TC-347）
1. 登录后 → 退出 → 点浏览器后退
2. 检查：不显示Dashboard内容，被重定向到/login

### P1-7: 软键盘不遮挡输入（替代 TC-M17）
1. 手机模式下点击页面底部的输入框
2. 检查：页面自动滚动让输入框可见（不被键盘遮挡）

### P1-8: 多条通知标记已读（替代 TC-263）
1. 管理员登录
2. 触发两次不同的操作产生通知
3. 到通知页面 → 逐条点击 → 未读计数归零

### P1-9: 密码修改后旧密码失效
1. 用当前密码登录
2. 修改密码
3. 用旧密码尝试登录 → 应失败
4. 用新密码登录 → 应成功

### P1-10: 手机端快速操作（替代 TC-227）
1. 手机模式下登录→退出→登录→退出 连续快速3次
2. 不卡顿、不崩溃、无重复请求

---

## 四、测试方案

这10条补测用以下方式：
- P1-1、P1-9：curl + cookie（API直测）
- P1-2：curl并发（API直测）
- P1-3、P1-4、P1-5、P1-6、P1-7、P1-8、P1-10：浏览器操作

**能用 curl 就用 curl，比浏览器更稳定。**

## 五、输出要求

报告放到 📥_→2号/，文件名：`2026-06-28_5号_补测报告.md`

格式：通过/失败/跳过的总览表 + 逐条结果 + 失败截图。

完成后更新BOARD.md最新动态。**不需要等回复，独立完成。**
